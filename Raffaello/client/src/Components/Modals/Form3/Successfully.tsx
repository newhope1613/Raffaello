import CheckResultModal from "../../CheckResultModal/CheckResultModal";

const Successfully = () => {
  return (
    <div>
      <CheckResultModal image="V.svg" text={<>Ваш чек загружен, спасибо!</>} />
    </div>
  );
};

export default Successfully;
